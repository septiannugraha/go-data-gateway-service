# Docker Environment Configuration

# Server
PORT=8081
ENV=production
API_KEYS=demo-key-123,fusio-gateway-key

# Redis (using Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379

# Dremio (accessing host machine)
DREMIO_HOST=host.docker.internal
DREMIO_PORT=32010
DREMIO_USERNAME=your-dremio-username
DREMIO_PASSWORD=your-dremio-password

# BigQuery
BIGQUERY_PROJECT_ID=spse-prod-sa
BIGQUERY_DATASET_ID=
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/spse-prod-sa-b00ef9fc8ed4.json

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=change-this-password
