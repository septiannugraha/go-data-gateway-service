# Docker Environment Configuration Example
# Copy this to .env.docker and update with your actual values

# Server
PORT=8081
ENV=production
API_KEYS=your-api-key-here,another-key-here

# Redis (using Docker service name)
REDIS_HOST=redis
REDIS_PORT=6379

# Dremio (accessing host machine)
DREMIO_HOST=host.docker.internal
DREMIO_PORT=32010
DREMIO_USERNAME=your_username_here
DREMIO_PASSWORD=your_password_here

# BigQuery
BIGQUERY_PROJECT_ID=your-project-id
BIGQUERY_DATASET_ID=
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account.json

# Monitoring
GRAFANA_USER=admin
GRAFANA_PASSWORD=change_this_password