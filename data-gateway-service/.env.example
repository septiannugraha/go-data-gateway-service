# Go Data Gateway Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=8081
ENV=production

# API Keys (comma-separated)
# Generate strong keys for production: openssl rand -base64 32
API_KEYS=demo-key-123,fusio-gateway-key,test-key-456

# Rate Limiting (requests per minute per API key)
RATE_LIMIT=100

# ============================================
# REDIS CONFIGURATION (Caching)
# ============================================
# For Docker: use "redis" as host
# For local: use "localhost"
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ============================================
# DREMIO CONFIGURATION (Apache Iceberg/Arrow Flight)
# ============================================
# For Docker: use "host.docker.internal" to access host Dremio
# For local: use "localhost" or actual hostname
DREMIO_HOST=host.docker.internal
DREMIO_PORT=32010
DREMIO_USERNAME=your-dremio-username
DREMIO_PASSWORD=your-dremio-password
# Or use token instead of username/password
# DREMIO_TOKEN=your-dremio-token

# ============================================
# BIGQUERY CONFIGURATION
# ============================================
# Google Cloud Project ID (e.g., gtp-data-prod)
BIGQUERY_PROJECT_ID=your-gcp-project-id

# Optional: Default dataset (leave empty if using fully qualified table names)
BIGQUERY_DATASET_ID=

# Path to service account JSON file
# For Docker: /app/credentials/bigquery-key.json (mounted volume)
# For local: ./bigquery/your-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/bigquery-key.json

# ============================================
# MONITORING (Optional - for docker-compose)
# ============================================
# Grafana admin credentials
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# ============================================
# PRODUCTION TIPS
# ============================================
# 1. Use strong API keys in production
# 2. Set Redis password for security
# 3. Use environment-specific files (.env.production, .env.staging)
# 4. Never commit .env files to git (already in .gitignore)
# 5. Monitor cache hit rates via /cache/stats endpoint
# 6. Check /metrics for Prometheus metrics
# 7. Access Grafana at http://localhost:3000