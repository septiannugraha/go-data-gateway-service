apiVersion: v1
kind: Service
metadata:
  name: docs-service
  namespace: prod
  labels:
    app: docs
    component: documentation
spec:
  selector:
    app: docs
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP